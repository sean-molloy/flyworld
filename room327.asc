// room script file

function room_Load()
{
fadeOutMusic();
}


function room_AfterFadeIn()
{
  
  aToothbrush.Play(eAudioPriorityHigh, eOnce);
  oToothBrush.TweenX(0.07, 173, eEaseLinearTween, eReverseRepeatTween);
 // cUPaul.SayAt(0, 10, 360, "&11 so we can continue in our roles as perfect little consumers.");
  WaitSeconds(2.0);
  aToothbrush.Stop();
  Cutscenes.nextCutSceneShot();
  //cutSceneShot = 14;
 // player.ChangeRoom(316);
  //Display(String.Format("%d", cutSceneShot));
}