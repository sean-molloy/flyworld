// new module header
import function turnStreetLightsOn(bool turnOn);
import function streetLightSquare(int centerX, int centerY, int squareSize);
import function streetLight(int x, int y);

//maybe just don't put the lights so close together?
  //or alternate light areas where you can define a whole box with max brightness