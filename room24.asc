// room script file

function room_Load()
{
  if(characterChange)
  {
    Game.Camera.SetSize(360, 200);
    Game.Camera.SetAt(262, 92);
  } else {
    Game.Camera.SetSize(596, 331);
    Game.Camera.SetAt(0, 0);
    player.Baseline = -1;
    playerLeftie();
    player.PlaceOnWalkableArea();
  }
}


function room_AfterFadeIn()
{
  if(characterChange)
  {
    characterChange = false;
  } else {
    Game.Camera.TweenSize(0.4, 360, 200, eEaseInOutSineTween, eNoBlockTween);
    Game.Camera.TweenPosition(0.4,  262, 92, eEaseInOutSineTween, eBlockTween);
  }
}

function region1_WalksOnto()
{
  player.ChangeRoom(23);
}

function hHotspot1Exit_AnyClick()
{
switch (verbUsed)
  {
    case 0: //walk
      player.GoTo(330, 220);
    break;
    case 1://look
      player.FaceLocation(330, 220, eBlock);
      lookAbiPaul("That's the way out.", "It goes back to the street.");
    break;
    case 2://use
      Canned.noUsing();
    break;
    case 3://talk
      Canned.noTalking();
    break;
    case 4://move
      Canned.noMoving();
    break;
    case 5://fourletters
      Canned.fuckAnyone();
    break;
    case 6://take
      Canned.cantTake();
    break;
    case 7://inv
      Canned.wontWork();
    break;
  }
  verbUsed = 0;
}
