// room script file

function room_AfterFadeIn()
{
  //WaitSeconds(0.1);
  /*
  
  if(musicTrack!=null)
      {     
       
       //musicTrack.TweenSpeed(0.5, -5, eEaseLinearTween, eBlockTween);
       //WaitSeconds(0.1);
       musicTrack.Stop();
      }
      */
      oLights.TweenFadeOut(1.0, eEaseOutSineTween, eBlockTween);
      //aMugBreak1.Play(eAudioPriorityHigh, eOnce);
        WaitSeconds(0.2);
  cutSceneShot = 8;
  player.ChangeRoom(eCSR_abiSneaks);
}
